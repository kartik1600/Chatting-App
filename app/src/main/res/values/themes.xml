<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ChattingApp" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ChattingApp" parent="Base.Theme.ChattingApp" />

    <style name="editTextStyle" parent="">
        <item name="android:maxLines">1</item>
        <item name="android:background">@drawable/bg_corner_round_edit_text</item>
        <item name="android:paddingVertical">@dimen/_10sdp</item>
        <item name="android:paddingHorizontal">@dimen/_10sdp</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="buttonStyle" parent="">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/bg_corner_round_button_blue</item>

    </style>
</resources>